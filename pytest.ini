[pytest]
addopts = -ra
markers =
    unit: Unit tests (no external I/O)
    integration: Full-stack HTTP integration tests
    security: Adversarial / security tests
    smoke: Fast smoke tests for CI gate
